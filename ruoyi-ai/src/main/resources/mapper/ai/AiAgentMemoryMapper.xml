<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ai.mapper.AiAgentMemoryMapper">

    <resultMap type="AiAgentMemory" id="AiAgentMemoryResult">
        <id     property="memoryId"        column="memory_id"          />
        <result property="memoryType"      column="memory_type"        />
        <result property="memoryKey"       column="memory_key"         />
        <result property="memoryTitle"     column="memory_title"       />
        <result property="toolName"        column="tool_name"          />
        <result property="argumentsJson"   column="arguments_json"     />
        <result property="resultJson"      column="result_json"        />
        <result property="summary"         column="summary"            />
        <result property="sessionId"       column="session_id"         />
        <result property="operName"        column="oper_name"          />
        <result property="sourceChatLogId" column="source_chat_log_id" />
        <result property="expireTime"      column="expire_time"        />
        <result property="status"          column="status"             />
        <result property="createTime"      column="create_time"        />
        <result property="updateTime"      column="update_time"        />
        <result property="remark"          column="remark"             />
    </resultMap>

    <sql id="selectAgentMemoryVo">
        select memory_id, memory_type, memory_key, memory_title, tool_name, arguments_json, result_json,
               summary, session_id, oper_name, source_chat_log_id, expire_time, status,
               create_time, update_time, remark
        from ai_agent_memory
    </sql>

    <select id="selectLatestValidMemory" parameterType="AiAgentMemory" resultMap="AiAgentMemoryResult">
        <include refid="selectAgentMemoryVo"/>
        <where>
            status = '0'
            and (expire_time is null or expire_time &gt; sysdate())
            <if test="operName != null and operName != ''">and oper_name = #{operName}</if>
            <if test="memoryType != null and memoryType != ''">and memory_type = #{memoryType}</if>
            <if test="memoryKey != null and memoryKey != ''">and memory_key = #{memoryKey}</if>
            <if test="sessionId != null and sessionId != ''">and session_id = #{sessionId}</if>
        </where>
        order by create_time desc, memory_id desc
        limit 1
    </select>

    <insert id="insertAgentMemory" parameterType="AiAgentMemory" useGeneratedKeys="true" keyProperty="memoryId">
        insert into ai_agent_memory(
            memory_type, memory_key, memory_title, tool_name, arguments_json, result_json,
            summary, session_id, oper_name, source_chat_log_id, expire_time, status,
            create_time, update_time, remark
        )values(
            #{memoryType}, #{memoryKey}, #{memoryTitle}, #{toolName}, #{argumentsJson}, #{resultJson},
            #{summary}, #{sessionId}, #{operName}, #{sourceChatLogId}, #{expireTime}, #{status},
            sysdate(), sysdate(), #{remark}
        )
    </insert>
</mapper>
