现在做到了生产领料阶段，但是在出库单中还有一些东西需要优化：
1.出库单的物料明细列表中，输入批次，我需要下拉框提供出对应物料的所有批次让我选择

2026-06-01 已完成：
1.出库单物料明细批次已改为按仓库+物料联动的下拉选择。
2.新增销售订单、销售订单明细、裁剪单、裁剪单明细、发货单相关前后端模块与升级SQL。
3.生产订单已增加“生成裁剪单”入口，后端会校验生产领料出库单已过账后才允许创建裁剪单。
4.裁剪单状态使用int：0待裁剪、1裁剪中、2已完成、3已取消；列表中超时单按计划完成时间自动识别、靠前显示并标红。
5.生产订单创建裁剪单后会自动改为已完成，生产领料出库单同步改为已完成；前后端均禁止已完成生产单继续领料，禁止已完成出库单取消过账、再次过账或保存修改。
